steps: 
# Build
- name: 'gcr.io/cloud-builders/docker'
  args: [
      'build', 
      '-t', 
      'gcr.io/$PROJECT_ID/cloud-run-actix',
      '--cache-from', 'gcr.io/$PROJECT_ID/cloud-run-actixe:latest',
      '.']
images: ['gcr.io/$PROJECT_ID/cloud-run-actix']
timeout: 20m
